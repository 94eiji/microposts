<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      The form contrains <%= pluralize(object.errors.count, "error") %>
    </div>
    <ul>
      
      <!--エラメッセージと取り出して、リストで表示 -->
      <% object.errors.full_messages.each do |full_message| %>
        <li><%= full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>